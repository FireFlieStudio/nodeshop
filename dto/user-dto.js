module.exports = (user) =>{
        return {
            userID:user.userID,
            userName:user.userName,
            email:user.email,
            image:user.image,
            desc:user.desc
        }
    }
