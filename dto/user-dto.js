module.exports = (user) =>{
        return {
            userId:user.userId,
            userName:user.userName,
            emial:user.emial,
            image:user.image,
            desc:user.desc
        }
    }
